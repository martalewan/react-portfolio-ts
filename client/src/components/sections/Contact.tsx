import SectionTitle from "../modules/SectionTitle"

const Contact = () => {
    return (
        <section id="contact" className="page-padding">
            <SectionTitle label="04." title="Get in touch with me" subtitle="Hire me and just tell me hello" />
        </section>
    )
}

export default Contact